
- p1 1
I want to create procedural generation software using javascript and java. frontend will be react with mui. backend will be java with spring boot. database will be sqlite. project is procgenjs and it may be called procgen. do all for folder procgenjs. other fodler outside are other project
- workspace - it should be able to have multiple workspace. each workspace will have its own database list file that tells all database to be loaded, files folder where all files are stored, asset folder that may contain images scripts themes libraries etc, configuration folder that may contain configuration files, workspace file that describes all configuration for that workspae. procgenjs should be able to support multiple workspace. it should load multiple workspace, also provide dashboard for workspace management to enable disable worksapce so only enable worksapce are loaded. for creating workspace create a base workspace and process to copy it to given folder and change all details in that created workspace.
- datasource - it should be able to have multiple datasource. for database the main database is loaded and multiple databse can be configured with name of database so procgenjs can load multiple workspace and multiple database within that workspace. also other datasource can be used like file system, json as database, etc. javas should provide way to dynamically configure database when loading workspace or creating or deleting. 
- project system - it should be able to have multiple project. each project will have its own database list file that tells all database to be loaded, files folder where all files are stored, asset folder that may contain images scripts themes libraries etc, configuration folder that may contain configuration files, project file that describes all configuration for that project. procgenjs should be able to support multiple project. it should load multiple project, also provide dashboard for project management to enable disable project so only enable project are loaded. each project is created inside files folder project folder.
- procgenjs general ui structure - procgenjs should have general ui structure that can be used by all projects. it should have dashboard, project management, workspace management, datasource management, etc. it should list different type of services available and create a tab for given service within that rwoser page. it should support multiple tabs within that browser pag and maintaining context of each tab efficiently so that they dont override each other. I can have different service like procedural paint application, file editor, image editor, code editor, game creator, host other service, music editor, video editor, ai application, lego builders, content creation etc so manage this during runtime, also the codebase should have proper management of code in procgenjs project so that new service can be easily added, updated, removed. the general ui structure should be able to provide panel that can be used to load files for service it could provide tree strucutre of files and folders and provide option to run scripts at runtime whatever type like javscript, custom script, markdown, html, text, configuration, music files, etc. like procedural paint application will provide file tree list that can load different files and run it so when a javascript file loaded it will paint in canvas by brush stroke or draw what is in code. paint application should also provide all paint image related features. 3d render service can provide different render service and dynamically run code to draw. the code should be able to run at runtime and provide output in canvas or other way.
- code wise keep code efficient
- java service - should provide ways to manage api, database, external configuration, modular, able to add new libraries fast and provide service through api, socket or other way. t should also provide way to host files from different workspace. it should be able to host other workspace. there should be common api that are used by overall project, service api used for some service etc.